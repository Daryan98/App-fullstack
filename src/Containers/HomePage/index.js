import React, { Component } from 'react'
// import PropTypes from 'prop-types'
import PageTemplate from '../../Components/PagesTemplate';

class HomePage extends Component {
  render(){
    return(
      <PageTemplate>
  
      </PageTemplate>
    )
  }
}

HomePage.propTypes = {}

export default HomePage;